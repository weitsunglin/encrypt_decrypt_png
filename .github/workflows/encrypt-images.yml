name: Encrypt Images

on: [push]

jobs:
  encrypt:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v2

    - name: Set up Python 3
      uses: actions/setup-python@v2
      with:
        python-version: 3.8 # 或者選擇你喜歡的其他Python 3版本

    - name: Install dependencies
      run: |
        # 如果有依賴可以在這裡安裝
        # pip install -r requirements.txt

    - name: Encrypt PNG Image
      run: python pngencode.py  # 確保你的腳本是Python 3兼容的

    - name: Commit and Push Encrypted Image
      run: |
        git config --local user.email "weitsunglin@github.com"
        git config --local user.name "GitHub Action"
        git add test.png
        git commit -m "Encrypt test.png" || echo "No changes to commit"
        git push
